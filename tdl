i) maybe replace continuous "chained" replaces into a single replace to boost 
     performance

ii) make LangSpecificCorrection() function more conscise to follow by removing several calls to replace
using a list, map and a loop to do the corrections instead

iii) handle file operation exceptions everywhere

iv) correction in SchwaSpecificCorrection() check



A) GetPhoneType - check later
B) Check from GeminateCorrection onwards in helpers.py and parser.py fully

